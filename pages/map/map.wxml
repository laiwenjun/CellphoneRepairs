<view class="mapCon">
    <map controls polyline showLocation covers="{{covers}}" id="myMap" latitude="{{latitude}}" longitude="{{longitude}}" markers="{{markers}}" scale="18" style="width: 100%; height: 100%;"></map>
</view>
